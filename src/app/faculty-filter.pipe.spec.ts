import { FacultyFilterPipe } from './faculty-filter.pipe';

describe('FacultyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FacultyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
